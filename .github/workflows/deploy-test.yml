name: 2. TEST Deploy
run-name: Executed by ${{ github.actor }} 🔍
on:
  workflow_dispatch:
    inputs:
      tag:
        description: Tag to deploy
        required: true
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying my_image:${{ github.event.inputs.change_type }}"
      - run: echo "🍏 This job's status is ${{ job.status }}."
